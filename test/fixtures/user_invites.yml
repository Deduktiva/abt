pending:
  token: pending-invite-token-fixture-aaaaaaaaaaaaaaaaa
  created_by_user: alice
  expires_at: <%= 1.hour.from_now.to_formatted_s(:db) %>
  note: "pending"

expired:
  token: expired-invite-token-fixture-bbbbbbbbbbbbbbbbb
  created_by_user: alice
  expires_at: <%= 1.hour.ago.to_formatted_s(:db) %>
  note: "expired"

consumed:
  token: consumed-invite-token-fixture-ccccccccccccccccc
  created_by_user: alice
  expires_at: <%= 1.day.from_now.to_formatted_s(:db) %>
  consumed_at: <%= 1.minute.ago.to_formatted_s(:db) %>
  consumed_by_user: bob
  note: "consumed"
